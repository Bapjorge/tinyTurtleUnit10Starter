TinyTurtle.apply(window);
stamp();

function shape(len,ang){
    forward(len);
    right(ang);
      forward(len);
    right(ang);
      forward(len);
    right(ang);
       forward(len);
    right(ang);
      forward(len);
    right(ang);
      forward(len);
    right(ang);

}

// Type your function call below

shape(60,90 );

